import type { Lesson } from '@/lib/data/courses'

export const lesson2_3: Lesson = {
  id: 'lesson-2-3',
  title: 'Platform-specifieke content optimalisatie',
  duration: '90 minuten',
  content: `# Platform-specifieke content optimalisatie

## Elk platform heeft zijn eigen taal

Een perfecte LinkedIn post kan volledig floppen op TikTok. Een viral Instagram Reel werkt niet op Twitter. Elk platform heeft zijn eigen cultuur, algoritme en best practices. Leer hoe je AI gebruikt om content perfect af te stemmen op elk platform.

## De anatomie van perfecte posts per platform

### LinkedIn: Het professionele podium

**Wat het algoritme beloont:**
- Dwell time (tijd die mensen besteden aan je post)
- Meaningful interactions (comments > likes)
- Native content (geen externe links in posts)
- Vroege engagement (eerste uur is cruciaal)

**De perfecte LinkedIn post formule:**

\`\`\`
Hook (regel 1-2): Controversiële stelling of intrigerende vraag
↓
Context (regel 3-5): Persoonlijke anekdote of case study
↓
Value (regel 6-15): 3-5 concrete takeaways
↓
CTA (regel 16-17): Vraag om meningen/ervaringen
↓
Hashtags (regel 18): Max 3-5 relevante tags
\`\`\`

**AI Prompt voor LinkedIn optimalisatie:**
\`\`\`
"Schrijf een LinkedIn post over [onderwerp] met:
- Hook die stopt met scrollen (controversieel/intrigerend)
- Persoonlijk verhaal of case study (100-150 woorden)
- 3-5 bullet points met concrete inzichten
- Single line breaks voor leesbaarheid
- Vraag die uitnodigt tot discussie
- 3 relevante hashtags

Tone: Professioneel maar persoonlijk
Lengte: 1300-1900 karakters (sweet spot)"
\`\`\`

### Instagram: Visueel storytelling platform

**Het Instagram algoritme in 2024:**
- Reels > Foto's > Carousels voor reach
- Saves + Shares > Comments > Likes
- Time spent op content
- Relationship signals (DMs, profile visits)

**Content types en hun optimalisatie:**

**Reels (Hoogste prioriteit):**
\`\`\`
AI Prompt: "Creëer een Reel script voor [product/service]:
- Hook in eerste 3 seconden
- Problem/solution narrative
- Quick transitions om retention te houden
- Text overlay ideas
- Trending audio suggestie
- CTA in caption EN video"
\`\`\`

**Carousel Posts:**
\`\`\`
AI Prompt: "Design een 10-slide carousel over [onderwerp]:
Slide 1: Intrigerende titel + belofte
Slide 2-3: Problem agitation
Slide 4-8: Step-by-step oplossing
Slide 9: Samenvatting/key takeaways
Slide 10: CTA + follow prompt

Per slide: Max 50 woorden"
\`\`\`

**Stories Optimalisatie:**
\`\`\`
- Polls/Questions voor engagement
- Behind-the-scenes content
- User-generated content reposting
- Countdown stickers voor launches
- Location tags voor local reach
\`\`\`

### TikTok: Entertainment-first platform

**TikTok's Voor Jou Pagina (FYP) Algoritme:**
- Completion rate > alles
- Rewatches zijn goud
- Snelle engagement (eerste 10 minuten)
- Native TikTok features gebruik

**De viral TikTok formule:**

\`\`\`
0-3 sec: Pattern interrupt (visueel of audio)
3-10 sec: Setup van het verhaal/probleem
10-40 sec: Value delivery met quick cuts
40-55 sec: Plot twist of unexpected ending
55-60 sec: Strong CTA

Audio: Trending sound met eigen twist
\`\`\`

**AI TikTok Script Generator:**
\`\`\`
"Schrijf een TikTok script (max 60 sec) over [onderwerp]:
- Opening hook: 'POV:', 'Story time:', of vraag
- 3-4 scene beschrijvingen met timing
- Text overlays per scene
- Trending hashtags (mix niche + broad)
- Comments bait in caption"
\`\`\`

### Twitter/X: Real-time conversation platform

**Twitter algoritme priorities:**
- Early engagement velocity
- Reply-to-engagement ratio
- Thread completion rate
- Media attachment performance

**Thread optimalisatie met AI:**
\`\`\`
"Maak een Twitter thread over [onderwerp]:
Tweet 1: Bold statement + preview wat komt
Tweet 2-7: Een insight per tweet (max 250 chars)
Tweet 8: Samenvatting met numbered list
Tweet 9: CTA (retweet als waardevol)

Regels:
- Elke tweet standalone waardevol
- Cliffhangers tussen tweets
- Visuele breaks (emoji's/bullets)"
\`\`\`

### YouTube: Long-form content koning

**YouTube's recommendation algoritme:**
- Click-through rate (thumbnail + titel)
- Average view duration
- Session duration (hoeveel videos kijkt iemand na de jouwe)
- Engagement (likes, comments, shares)

**AI voor YouTube optimalisatie:**
\`\`\`
"Optimaliseer deze YouTube video voor algoritme:
Onderwerp: [onderwerp]

Genereer:
1. 10 titel variaties (50-60 karakters)
   - Power words vooraan
   - Nummer/lijst format waar mogelijk
   
2. Thumbnail text ideas
   - Max 3-4 woorden
   - Contrasterende kleuren suggesties
   
3. Description template
   - Hook in eerste 125 karakters
   - Timestamps
   - Relevante links
   
4. Tags (mix broad + specific)
5. Eerste 15 seconden script"
\`\`\`

## Platform Cross-Promotion Strategie

### De Content Waterval Method:

\`\`\`
YouTube Video (Long-form)
    ↓
LinkedIn Article (Professional angle)
    ↓
Instagram Carousel (Visual summary)
    ↓
TikTok Series (Behind-scenes/outtakes)
    ↓
Twitter Thread (Key insights)
    ↓
Instagram Reels (Best moments)
\`\`\`

### AI Prompt voor Cross-Platform Adaptatie:
\`\`\`
"Ik heb deze content: [originele content]

Adapteer voor:
1. LinkedIn: Professional insights angle
2. Instagram: Visual storytelling approach  
3. TikTok: Entertainment/educational mix
4. Twitter: Snackable insights thread
5. YouTube Community: Discussion starter

Behoud kernboodschap maar pas tone/format aan per platform"
\`\`\`

## Advanced Platform Hacks met AI

### LinkedIn Algorithm Hacks:
1. **Dwell Time Maximizer:**
   - Gebruik "See more..." strategisch
   - Plaats belangrijkste info na de fold
   - Single line breaks voor scanning

2. **Pod Engagement Strategie:**
   \`\`\`
   AI Prompt: "Genereer 10 verschillende eerste comments 
   die ik kan gebruiken op posts van connections.
   Moet: waardevol, authentiek, discussie-startend zijn"
   \`\`\`

### Instagram Growth Hacks:
1. **Hashtag Layering:**
   \`\`\`
   - 5 Branded hashtags (< 1k posts)
   - 10 Niche hashtags (1k-100k posts)  
   - 10 Broad hashtags (100k-1M posts)
   - 5 Trending hashtags (check dagelijks)
   \`\`\`

2. **Story Highlights Optimalisatie:**
   - FAQ's
   - Testimonials
   - Behind the Scenes
   - Tutorials
   - Products/Services

### TikTok Viral Triggers:
1. **Hook Variaties Testen:**
   \`\`\`
   Test deze openings:
   - "Nobody talks about..."
   - "I was today years old when..."
   - "POV: You just discovered..."
   - "Wait till the end for..."
   - "The [industry] doesn't want you to know..."
   \`\`\`

2. **Comment Bait Strategies:**
   - Bewust kleine "fout" maken
   - Controversiële mening delen
   - "Comment [emoji] if you agree"
   - Cliffhanger zonder oplossing

## Platform-Specifieke AI Tools

### LinkedIn Tools:
- **Shield Analytics**: Track post performance
- **Taplio**: AI writing assistant voor LinkedIn
- **AuthoredUp**: Post formatting en analytics

### Instagram Tools:
- **Later**: Visual content calendar
- **Flick**: AI hashtag research
- **Creator Studio**: Native analytics

### TikTok Tools:
- **TikTok Creative Center**: Trend discovery
- **Tokboard**: Analytics en competitor research
- **CapCut**: Native video editing met AI features

### Multi-Platform Tools:
- **Hootsuite**: Cross-platform scheduling
- **Sprout Social**: Unified inbox en analytics
- **Buffer**: Simple scheduling met AI insights

## Praktijkcase: Platform-specifieke campagne

**Opdracht:** Launch nieuwe product lijn

**LinkedIn Strategie:**
- Thought leadership posts van CEO
- Employee advocacy programma
- Case studies van early adopters
- Industry trend analyses

**Instagram Strategie:**
- Behind-the-scenes van product ontwikkeling
- User-generated content campaign
- Influencer unboxing videos
- Stories takeover door team

**TikTok Strategie:**
- Product in actie challenges
- Voor/na transformaties
- Funny fails compilatie
- Educational "Did you know" series

**Resultaten:**
- LinkedIn: 450% meer B2B leads
- Instagram: 320% follower groei
- TikTok: 2.3M views, 89K nieuwe followers

## Platform Optimalisatie Checklist

### Pre-Publishing:
- [ ] Platform-specifieke formatting
- [ ] Optimale media resolutie
- [ ] Trending hashtags research
- [ ] Best posting time check
- [ ] A/B test varianten ready

### Post-Publishing:
- [ ] Eerste 30 min: actief reageren
- [ ] Cross-promote op Stories/andere platforms
- [ ] Engagement pods activeren
- [ ] Performance tracking setup
- [ ] Repurpose planning voor top performers`,
  codeExamples: [
    {
      id: 'example-1',
      title: 'Multi-Platform Content Optimizer',
      language: 'python',
      code: `import openai
import re
from typing import Dict, List
from datetime import datetime
import json

class PlatformOptimizer:
    def __init__(self, api_key):
        self.client = openai.OpenAI(api_key=api_key)
        self.platform_specs = {
            'linkedin': {
                'char_limit': 3000,
                'hashtag_limit': 5,
                'media_types': ['image', 'video', 'document', 'article'],
                'best_times': ['07:30', '12:00', '17:30'],
                'tone': 'professional, insightful'
            },
            'instagram': {
                'caption_limit': 2200,
                'hashtag_limit': 30,
                'media_types': ['image', 'video', 'reels', 'carousel'],
                'best_times': ['06:00', '12:00', '19:00'],
                'tone': 'casual, visual, engaging'
            },
            'tiktok': {
                'caption_limit': 300,
                'hashtag_limit': 10,
                'media_types': ['video'],
                'best_times': ['06:00', '10:00', '19:00', '22:00'],
                'tone': 'entertaining, trendy, fast-paced'
            },
            'twitter': {
                'char_limit': 280,
                'hashtag_limit': 2,
                'media_types': ['image', 'video', 'gif'],
                'best_times': ['08:00', '13:00', '18:00'],
                'tone': 'concise, witty, conversational'
            }
        }
    
    def optimize_for_platform(self, content: str, platform: str, content_type: str):
        """Optimaliseer content voor specifiek platform"""
        
        spec = self.platform_specs.get(platform.lower())
        if not spec:
            return "Platform not supported"
        
        prompt = f"""
        Optimaliseer deze content voor {platform}:
        
        Originele content: {content}
        Content type: {content_type}
        
        Platform specificaties:
        - Karakter limiet: {spec['char_limit']}
        - Hashtag limiet: {spec['hashtag_limit']}
        - Tone: {spec['tone']}
        
        Genereer:
        1. Geoptimaliseerde post/caption
        2. {spec['hashtag_limit']} relevante hashtags
        3. Engagement hook voor eerste zin
        4. Call-to-action
        5. Emoji suggesties waar passend
        
        Format: JSON
        """
        
        response = self.client.chat.completions.create(
            model="gpt-4",
            messages=[{"role": "user", "content": prompt}],
            temperature=0.7
        )
        
        return json.loads(response.choices[0].message.content)
    
    def generate_platform_variants(self, core_message: str):
        """Genereer varianten voor alle platforms van één kernboodschap"""
        
        variants = {}
        
        for platform in self.platform_specs.keys():
            prompt = f"""
            Kernboodschap: {core_message}
            
            Creëer een {platform}-specifieke versie:
            
            Vereisten voor {platform}:
            {json.dumps(self.platform_specs[platform], indent=2)}
            
            Include:
            - Platform-native format
            - Optimale hashtag strategie  
            - Engagement triggers
            - Visual/media suggesties
            """
            
            response = self.client.chat.completions.create(
                model="gpt-4",
                messages=[{"role": "user", "content": prompt}],
                temperature=0.8
            )
            
            variants[platform] = response.choices[0].message.content
        
        return variants
    
    def analyze_hashtag_performance(self, hashtags: List[str], platform: str):
        """Analyseer hashtag performance potentieel"""
        
        prompt = f"""
        Analyseer deze hashtags voor {platform}:
        {', '.join(hashtags)}
        
        Voor elke hashtag, geef:
        1. Geschatte bereik (high/medium/low)
        2. Competitie niveau
        3. Relevantie score (1-10)
        4. Alternatieve suggesties
        
        Recommend optimale mix van:
        - Branded hashtags
        - Niche hashtags
        - Trending hashtags
        - Evergreen hashtags
        """
        
        response = self.client.chat.completions.create(
            model="gpt-4",
            messages=[{"role": "user", "content": prompt}]
        )
        
        return response.choices[0].message.content
    
    def create_content_calendar(self, topics: List[str], platforms: List[str], days: int = 30):
        """Maak platform-specifieke content kalender"""
        
        calendar = {}
        
        for day in range(days):
            date = datetime.now().strftime(f"Day {day + 1}")
            calendar[date] = {}
            
            for platform in platforms:
                topic = topics[day % len(topics)]
                spec = self.platform_specs[platform]
                
                # Bepaal beste tijd voor deze dag
                best_time = spec['best_times'][day % len(spec['best_times'])]
                
                calendar[date][platform] = {
                    'topic': topic,
                    'time': best_time,
                    'content': self.optimize_for_platform(topic, platform, 'general'),
                    'media_suggestion': self.suggest_media(platform, topic)
                }
        
        return calendar
    
    def suggest_media(self, platform: str, topic: str):
        """Suggest platform-appropriate media"""
        
        media_strategies = {
            'linkedin': {
                'document': 'Industry report or whitepaper',
                'infographic': 'Data visualization',
                'video': 'Expert interview or case study'
            },
            'instagram': {
                'carousel': 'Step-by-step guide',
                'reels': 'Quick tips or transformation',
                'stories': 'Behind-the-scenes or polls'
            },
            'tiktok': {
                'video': 'Tutorial, challenge, or trending format',
                'effects': 'Use platform effects and filters',
                'sound': 'Trending audio recommendation'
            }
        }
        
        return media_strategies.get(platform, {})

# Advanced platform-specific features
class AdvancedPlatformFeatures:
    def __init__(self, optimizer):
        self.optimizer = optimizer
    
    def linkedin_algorithm_optimizer(self, post: str):
        """LinkedIn-specifieke optimalisaties"""
        
        optimizations = {
            'dwell_time_tricks': [
                'Use "See more..." strategically',
                'Place key insights after line 3',
                'Single line breaks for readability'
            ],
            'engagement_boosters': [
                'End with thought-provoking question',
                'Tag relevant people (max 3)',
                'Post at peak professional hours'
            ],
            'format_suggestions': self._linkedin_format_check(post)
        }
        
        return optimizations
    
    def instagram_reel_optimizer(self, concept: str):
        """Instagram Reels specifieke optimalisatie"""
        
        prompt = f"""
        Reel concept: {concept}
        
        Genereer:
        1. Hook variations (first 3 seconds)
        2. Scene breakdown met timings
        3. Text overlay suggestions
        4. Trending audio matches
        5. Effect/filter recommendations
        6. Thumbnail frame suggestion
        
        Focus op: High retention rate
        """
        
        return self.optimizer.client.chat.completions.create(
            model="gpt-4",
            messages=[{"role": "user", "content": prompt}]
        ).choices[0].message.content
    
    def tiktok_viral_analyzer(self, video_idea: str):
        """TikTok viral potential analyse"""
        
        viral_elements = {
            'hook_strength': self._analyze_hook(video_idea),
            'trend_alignment': self._check_trend_fit(video_idea),
            'shareability': self._calculate_share_potential(video_idea),
            'comment_bait': self._generate_comment_triggers(video_idea)
        }
        
        return viral_elements
    
    def _linkedin_format_check(self, post: str):
        """Check LinkedIn formatting best practices"""
        
        issues = []
        
        # Check line length
        lines = post.split('\\n')
        for i, line in enumerate(lines):
            if len(line) > 125:
                issues.append(f"Line {i+1} too long for mobile")
        
        # Check emoji usage
        emoji_count = len(re.findall(r'[😀-🙏]', post))
        if emoji_count > 5:
            issues.append("Too many emojis for LinkedIn")
        
        # Check hashtag placement
        if '#' in post[:-100]:
            issues.append("Hashtags should be at the end")
        
        return issues

# Gebruik
optimizer = PlatformOptimizer("your-api-key")

# Optimaliseer één boodschap voor alle platforms
core_message = "AI transformeert hoe we content creëren"
variants = optimizer.generate_platform_variants(core_message)

# Platform-specifieke optimalisatie
linkedin_post = optimizer.optimize_for_platform(
    "Nieuw onderzoek toont aan dat AI tools productiviteit met 40% verhogen",
    "linkedin",
    "thought_leadership"
)

# Maak multi-platform kalender
topics = ["AI trends", "Productivity tips", "Case studies", "Tool reviews"]
calendar = optimizer.create_content_calendar(topics, ["linkedin", "instagram"], 30)`,
      explanation: 'Complete Python class voor het optimaliseren van content voor verschillende social media platforms met AI.'
    },
    {
      id: 'example-2',
      title: 'Platform Algorithm Optimizer',
      language: 'javascript',
      code: `class PlatformAlgorithmOptimizer {
  constructor(apiKey) {
    this.apiKey = apiKey;
    this.algorithms = this.loadAlgorithmSpecs();
  }

  loadAlgorithmSpecs() {
    return {
      linkedin: {
        signals: {
          dwellTime: { weight: 0.35, optimize: 'maximize' },
          comments: { weight: 0.25, optimize: 'maximize' },
          shares: { weight: 0.20, optimize: 'maximize' },
          likes: { weight: 0.10, optimize: 'moderate' },
          clicks: { weight: 0.10, optimize: 'minimize' }
        },
        features: {
          nativeVideo: 1.3,
          document: 1.2,
          externalLinks: 0.7,
          hashtags: 1.1,
          mentions: 1.15
        }
      },
      instagram: {
        signals: {
          saves: { weight: 0.30, optimize: 'maximize' },
          shares: { weight: 0.25, optimize: 'maximize' },
          comments: { weight: 0.20, optimize: 'maximize' },
          likes: { weight: 0.15, optimize: 'moderate' },
          profileVisits: { weight: 0.10, optimize: 'maximize' }
        },
        features: {
          reels: 1.5,
          carousel: 1.2,
          singleImage: 1.0,
          stories: 1.1,
          igtv: 0.9
        }
      },
      tiktok: {
        signals: {
          completionRate: { weight: 0.40, optimize: 'maximize' },
          rewatches: { weight: 0.25, optimize: 'maximize' },
          shares: { weight: 0.20, optimize: 'maximize' },
          comments: { weight: 0.10, optimize: 'maximize' },
          likes: { weight: 0.05, optimize: 'moderate' }
        },
        features: {
          trendingSound: 1.4,
          effects: 1.2,
          originalSound: 1.1,
          duet: 1.3,
          stitch: 1.25
        }
      }
    };
  }

  async optimizeContent(content, platform, goals) {
    const algorithm = this.algorithms[platform];
    if (!algorithm) throw new Error(\`Platform \${platform} not supported\`);

    const optimization = {
      content: await this.optimizeForAlgorithm(content, algorithm, platform),
      timing: this.calculateOptimalTiming(platform, goals),
      features: this.recommendFeatures(platform, content),
      predictions: await this.predictPerformance(content, platform)
    };

    return optimization;
  }

  async optimizeForAlgorithm(content, algorithm, platform) {
    const prompt = \`
    Optimize this content for \${platform}'s algorithm:
    
    Content: \${content}
    
    Algorithm priorities:
    \${JSON.stringify(algorithm.signals, null, 2)}
    
    Optimize for:
    1. Maximum dwell time/completion rate
    2. High engagement triggers
    3. Share-worthy moments
    4. Comment bait elements
    5. Platform-native features
    
    Return optimized version with explanations.
    \`;

    const response = await this.callAI(prompt);
    return this.parseOptimizedContent(response);
  }

  calculateOptimalTiming(platform, goals) {
    const timingStrategies = {
      linkedin: {
        b2b: {
          weekdays: ['07:30', '12:00', '17:30'],
          avoid: ['friday_afternoon', 'weekends']
        },
        b2c: {
          weekdays: ['08:00', '12:30', '18:00'],
          weekends: ['10:00', '14:00']
        }
      },
      instagram: {
        global: {
          peak: ['11:00-13:00', '17:00-21:00'],
          stories: ['08:00', '13:00', '21:00']
        },
        local: {
          morning: '06:00-09:00',
          lunch: '11:30-13:30',
          evening: '17:00-22:00'
        }
      },
      tiktok: {
        viral: {
          slots: ['06:00', '10:00', '19:00', '22:00'],
          avoid: ['02:00-05:00']
        }
      }
    };

    return timingStrategies[platform] || {};
  }

  async predictPerformance(content, platform) {
    const features = this.extractContentFeatures(content);
    const algorithmScore = this.calculateAlgorithmScore(features, platform);
    
    const prediction = {
      estimatedReach: this.estimateReach(algorithmScore, platform),
      engagementRate: this.predictEngagement(features, platform),
      viralPotential: this.calculateViralScore(features, platform),
      recommendations: await this.generateImprovements(content, platform)
    };

    return prediction;
  }

  extractContentFeatures(content) {
    return {
      length: content.length,
      hasQuestion: content.includes('?'),
      hasEmoji: /[😀-🙏]/.test(content),
      hasHashtag: content.includes('#'),
      hasMention: content.includes('@'),
      hasUrl: /https?:\\/\\//.test(content),
      sentiment: this.analyzeSentiment(content),
      readability: this.calculateReadability(content)
    };
  }

  calculateAlgorithmScore(features, platform) {
    const algorithm = this.algorithms[platform];
    let score = 1.0;

    // Apply feature multipliers
    Object.entries(features).forEach(([feature, value]) => {
      if (algorithm.features[feature]) {
        score *= algorithm.features[feature];
      }
    });

    return Math.min(score, 2.0); // Cap at 2x boost
  }

  async generateImprovements(content, platform) {
    const improvements = [];
    const features = this.extractContentFeatures(content);

    // Platform-specific improvements
    switch(platform) {
      case 'linkedin':
        if (!features.hasQuestion) {
          improvements.push('Add engaging question at the end');
        }
        if (features.length < 1000) {
          improvements.push('Expand content for better dwell time');
        }
        break;
        
      case 'instagram':
        if (!features.hasEmoji) {
          improvements.push('Add emojis for visual appeal');
        }
        if (features.hasUrl) {
          improvements.push('Move link to bio or first comment');
        }
        break;
        
      case 'tiktok':
        if (features.length > 150) {
          improvements.push('Shorten caption for better retention');
        }
        improvements.push('Use trending sound for 40% reach boost');
        break;
    }

    return improvements;
  }

  // Real-time optimization features
  async realtimeOptimize(postId, platform, currentMetrics) {
    const optimization = {
      shouldBoost: this.analyzeBoostPotential(currentMetrics),
      nextActions: await this.suggestNextActions(currentMetrics, platform),
      a_b_test: this.generateABVariant(postId, currentMetrics)
    };

    return optimization;
  }

  analyzeBoostPotential(metrics) {
    const earlyEngagement = metrics.engagement_first_hour || 0;
    const benchmarks = {
      linkedin: 50,
      instagram: 100,
      tiktok: 500
    };

    return {
      shouldBoost: earlyEngagement > benchmarks[metrics.platform],
      boostBudget: this.calculateBoostBudget(earlyEngagement),
      timing: 'within_2_hours'
    };
  }
}

// Advanced cross-platform strategy
class CrossPlatformStrategy {
  constructor(optimizer) {
    this.optimizer = optimizer;
  }

  async createWaterfallStrategy(originalContent, primaryPlatform) {
    const strategy = {
      primary: await this.optimizer.optimizeContent(
        originalContent, 
        primaryPlatform, 
        { priority: 'maximum_reach' }
      ),
      secondary: {},
      repurpose: {}
    };

    // Create platform-specific variants
    const platforms = ['linkedin', 'instagram', 'tiktok', 'twitter'];
    
    for (const platform of platforms) {
      if (platform !== primaryPlatform) {
        strategy.secondary[platform] = await this.adaptContent(
          originalContent,
          primaryPlatform,
          platform
        );
      }
    }

    // Schedule repurpose content
    strategy.repurpose = this.scheduleRepurpose(strategy);

    return strategy;
  }

  async adaptContent(content, fromPlatform, toPlatform) {
    const adaptations = {
      'linkedin-to-instagram': {
        format: 'carousel',
        visualize: true,
        shorten: true
      },
      'instagram-to-tiktok': {
        format: 'video',
        addTrend: true,
        makeEntertaining: true
      },
      'tiktok-to-linkedin': {
        format: 'article',
        addContext: true,
        professional: true
      }
    };

    const key = \`\${fromPlatform}-to-\${toPlatform}\`;
    const adaptation = adaptations[key] || { format: 'standard' };

    return this.applyAdaptation(content, adaptation);
  }

  scheduleRepurpose(strategy) {
    const schedule = {};
    const baseDate = new Date();

    // Primary platform: immediate
    schedule[strategy.primary.platform] = baseDate;

    // Secondary platforms: staggered
    Object.keys(strategy.secondary).forEach((platform, index) => {
      const delay = (index + 1) * 24; // 24 hour intervals
      const postDate = new Date(baseDate);
      postDate.setHours(postDate.getHours() + delay);
      
      schedule[platform] = {
        date: postDate,
        time: this.optimizer.calculateOptimalTiming(platform).peak[0]
      };
    });

    return schedule;
  }
}

// Usage example
const optimizer = new PlatformAlgorithmOptimizer('api-key');
const strategy = new CrossPlatformStrategy(optimizer);

// Optimize for specific platform
const linkedinOptimized = await optimizer.optimizeContent(
  "Just discovered an amazing productivity hack using AI",
  "linkedin",
  { audience: 'b2b', goal: 'thought_leadership' }
);

// Create cross-platform strategy
const waterfallStrategy = await strategy.createWaterfallStrategy(
  "Revolutionary AI tool changes how we work",
  "linkedin"
);

console.log('Optimization complete:', waterfallStrategy);`,
      explanation: 'Geavanceerde JavaScript class voor het optimaliseren van content op basis van platform-specifieke algoritmes.'
    }
  ],
  assignments: [
    {
      id: 'assignment-1',
      title: 'Platform Performance Audit',
      description: 'Analyseer je beste en slechtste posts van de afgelopen maand op 2 verschillende platforms. Identificeer patronen en creëer een optimalisatie strategie met AI voor betere performance.',
      difficulty: 'medium',
      type: 'project',
      hints: [
        'Kijk naar timing, format, en content type',
        'Analyseer engagement patterns',
        'Test verschillende hooks en CTA\'s',
        'Vergelijk platform-specifieke metrics'
      ]
    },
    {
      id: 'assignment-2',
      title: 'Cross-Platform Content Waterfall',
      description: 'Neem één piece of hero content en adapteer het voor minstens 4 verschillende platforms. Gebruik AI om platform-specifieke optimalisaties toe te passen en meet de resultaten.',
      difficulty: 'hard',
      type: 'project',
      hints: [
        'Start met je best performende platform',
        'Pas tone en format aan per platform',
        'Gebruik platform-native features',
        'Schedule met optimale timing per platform'
      ]
    }
  ],
  resources: [
    {
      title: 'Platform Algorithm Updates 2024',
      url: 'https://socialmediatoday.com/algorithm-updates',
      type: 'article'
    },
    {
      title: 'Cross-Platform Analytics Dashboard',
      url: 'https://analytics.platform.com',
      type: 'tool'
    },
    {
      title: 'The Science of Viral Content',
      url: 'https://viralcontentscience.com',
      type: 'research'
    }
  ]
}